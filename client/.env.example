# Copia para .env e preenche.
# No Vite, só variáveis com prefixo VITE_ ficam acessíveis no código do browser —
# por isso nunca colocar aqui nada secreto (a chave secreta da Stripe fica só no server).

VITE_STRIPE_PUBLISHABLE_KEY=""
VITE_API_URL="http://localhost:4000/api"
