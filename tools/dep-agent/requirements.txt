# SDK oficial do Google Gemini (camada de juizo)
google-genai

# Abrir Pull Requests atraves da API do GitHub
PyGithub

# Carregar o .env em testes locais (em producao usamos os Secrets)
python-dotenv