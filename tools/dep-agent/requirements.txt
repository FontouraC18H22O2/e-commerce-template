# Versoes fixadas (==) para builds reprodutiveis: o agente corre com
# exatamente as mesmas bibliotecas no teu PC e no Linux do GitHub Action.
# Para atualizar, muda a versao aqui de propria mao (com cuidado) - ou,
# um dia, deixa o proprio agente propor-te o bump.

# SDK oficial do Google Gemini (camada de juizo)
google-genai==2.23.0

# Abrir Pull Requests atraves da API do GitHub
PyGithub==2.10.0

# Carregar o .env em testes locais (em producao usamos os Secrets)
python-dotenv==1.2.3