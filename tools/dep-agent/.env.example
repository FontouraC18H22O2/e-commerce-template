# Copia este ficheiro para .env APENAS para testes locais.
# O .env real esta no .gitignore e nunca vai para o repositorio.
# Em producao, esta variavel vem dos Secrets do GitHub Actions.

ANTHROPIC_API_KEY=